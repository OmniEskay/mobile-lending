import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('MobileLending', () => App);
AppRegistry.runApplication('MobileLending', {
  rootTag: document.getElementById('root'),
}); 